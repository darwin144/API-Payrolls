@model IEnumerable<Client.Models.AccountRole>

@{
    ViewData["Title"] = "ListAccountRole";
    Layout = "_SidebarLayout";
}


@*<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Account_id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Role_id)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Account_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Role_id)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
*@


<div id="main">
	<header class="mb-3 ">
		<a href="#" class="burger-btn d-block d-xl-none">
			<i class="bi bi-justify fs-3"></i>
		</a>
	</header>

	<div class="page-heading">
		<div class="page-title">
			<div class="row">
				<div class="col-12 col-md-6 order-md-1 order-last">
					<h3>List Account Role</h3>
					<p class="text-subtitle text-muted">
						Manager Employee Data
					</p>
				</div>
				<div class="col-12 col-md-6 order-md-2 order-first">
					<nav aria-label="breadcrumb"
						 class="breadcrumb-header float-start float-lg-end">
						<ol class="breadcrumb">
							<li class="breadcrumb-item">
								<a href="index.html">Dashboard</a>
							</li>
							<li class="breadcrumb-item active" aria-current="page">
								Account Role
							</li>
						</ol>
					</nav>
				</div>
			</div>
		</div>
		<section class="section">
			<div class="card">
				<div class="">
					</br><a href="@Url.Action("CreateAccountRole","Admin")" class="btn btn-primary float-end me-4" data-toggle="modal" data-target="#modalTambah">
						Add Account Role
					</a>
				</div>

				<div class="card-body mw-100 text-sm" style="margin-end: -0.25rem !important;">
					
					<table class="table table-striped text-sm " style="margin-end: -0.25rem !important;" id="table1">
						<thead>
							<tr>
								<th>
									@Html.DisplayNameFor(model => model.Id)
								</th>
								<th>
									@Html.DisplayNameFor(model => model.Account_id)
								</th>
								<th>
									@Html.DisplayNameFor(model => model.Role_id)
								</th>

								<th>Action</th>
							</tr>
						</thead>
						
						<tbody>
							@foreach (var item in Model)
							{
								<tr>
									<td>
										@Html.DisplayFor(modelItem => item.Id)
									</td>
									<td>
										@Html.DisplayFor(modelItem => item.Account_id)
									</td>
									<td>
										@Html.DisplayFor(modelItem => item.Role_id)
									</td>
									<td>
										<a href="@Url.Action("EditAccountRole", new { id = item.Id })" class="btn btn-success">Edit</a>
										<a href="@Url.Action("DeleteAccountRole", new { id = item.Id })" class="btn btn-danger">Delete</a>
									</td>
								</tr>
							}
						</tbody>
					</table>
				</div>
			</div>
		</section>
	</div>

	<footer>
		<div class="footer clearfix mb-0 text-muted mt-auto px-4">
			<div class="float-start text-sm">
				<p>2023 &copy;</p>
			</div>
			<div class="float-end text-sm">
				<p>
					Project MCC by
					<span class="text-danger">
						<i class="bi bi-heart-fill icon-mid"></i>
					</span>
					<a href="">Kelompok 4</a>
				</p>
			</div>
		</div>
	</footer>
</div>
